- Make entire backup into a map(object({})) varible in backup module to enable more flexible backup change requirements from the root.

- Create monitoring module using cloudwatch to monitor backup and recovery activities in AWS

- Make backup times and counts into variables

- 